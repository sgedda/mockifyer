# Auth is built-in demo sign-in — no Clerk or OAuth vars required.
#
# Mockifyer (optional) — match react-native-expo-example / docs:
# EXPO_PUBLIC_MOCKIFYER_BACKEND=hybrid
# EXPO_PUBLIC_MOCKIFYER_RECORD=true
# EXPO_PUBLIC_MOCKIFYER_PROXY_URL=http://localhost:3002
# MOCKIFYER_MODE=on
