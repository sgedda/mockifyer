[phases.setup]
nixPkgs = ["nodejs-20_x", "npm-10_x"]

[phases.install]
cmds = [
  "cd example-projects/express-api-mock",
  "npm install",
  "npm run build"
]

[start]
cmd = "cd example-projects/express-api-mock && npm start"

