node_modules
dist
coverage
*.log
.env
.env.local
package-lock.json
package-lock.prod.json
# CRITICAL: Exclude persisted/ directory from build to prevent overwriting Railway volume
# If persisted/ is included in the build, nixpacks will copy empty files into the volume,
# overwriting existing vote data on deployment
persisted/
# Also exclude individual vote files (backup in case directory exclusion doesn't work)
feature-votes.json
user-votes.json
ip-votes.json
contact-submissions.json